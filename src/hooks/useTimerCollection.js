const UseTimerCollection = () => {
	// 사용자 정보(도큐먼트)에 타이머 콜렉션 생성
	const createTimerCollection = () => {};

	// 타이머 콜렉션에 타이머 도큐먼트 생성
	const addTimerDocument = () => {};

	// 타이머 콜렉션에 타이머 도큐먼트 수정
	const updateTimerDocument = () => {};

	return { createTimerCollection, addTimerDocument, updateTimerDocument };
};

export default UseTimerCollection;
